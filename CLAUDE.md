# Bernstein Agent: architect (architect-23971f1b)

This file was auto-generated by Bernstein for this agent session.
It provides task-specific instructions and constraints.

## Role constraints
You are a **architect** agent. Stay within your role's scope.

## Assigned tasks
### Task 1: Design Git worktree isolation architecture (id=3f78939bef55)
**Priority:** 2 | **Scope:** medium

Design the Git worktree isolation architecture for Source/Target in machinum-pipeline. Read existing code: packages/core/src/engine/git.ts (has execGit, autoCommit),  packages/core/src/uri.ts (has UriRegistry, ParsedUri, composite resolvers), packages/core/src/types.ts (Source/Target interfaces), packages/core/src/builtins/jsonl-source.ts (built-in pattern).
Design must support: 1. Composite scheme: git+jsonl://path?branch=work-branch to create git-worktree:// wrapper 2. Query parameter ?git=worktree on any URI to trigger worktree isolation 3. GitWorktreeSource: creates worktree from repo HEAD, then delegates to inner Source 4. GitWorktreeTarget: creates worktree, delegates to inner Target, then on close 
   auto-commits (if commit=on-close) and optionally merges to main
5. Cleanup/discard if pipeline fails 6. Reuse existing execGit function from engine/git.ts
Output: Write design document as docs/design/git-worktree-isolation.md with architecture diagram (text), interfaces, data flow, error handling, and implementation steps.


## Git rules
- Work on branch `agent/architect-23971f1b` only.
- NEVER force-push or skip git hooks.
- NEVER commit secrets, API keys, or credentials.
- Review changes with `git diff` before committing.
