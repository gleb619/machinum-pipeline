# Bernstein Agent: backend (backend-ca89aea8)

This file was auto-generated by Bernstein for this agent session.
It provides task-specific instructions and constraints.

## Role constraints
You are a **backend** agent. Stay within your role's scope.
- Focus on implementation, APIs, and data models.
- Write tests for new functionality.
- Follow existing code patterns and conventions.

## Assigned tasks
### Task 1: Add truncateString utility (id=ac637182f920)
**Priority:** 2 | **Scope:** medium

Add truncateString(text: string, maxLength: number, suffix?: string): string to packages/core/src/utils/string.ts. Truncate text to maxLength, append suffix (default '...') if truncated, handle edge cases (empty string, maxLength <= 0, text shorter than maxLength). Export from packages/core/src/index.ts.

## Allowed file paths
You should focus your work on these files:
- `packages/core/src/index.ts`
- `packages/core/src/utils/string.ts`

If you need to create new files, keep them within the directories of your allowed paths. Do not modify files outside your scope unless strictly necessary for your task.

## Git rules
- Work on branch `agent/backend-ca89aea8` only.
- NEVER force-push or skip git hooks.
- NEVER commit secrets, API keys, or credentials.
- Review changes with `git diff` before committing.
