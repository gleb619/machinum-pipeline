# Default tools configuration for empty-body-test
# This file is used by SetupCommand when no specific tools file is specified
# Copy or symlink to tools-empty.yaml, tools-minimal.yaml, or tools-partial.yaml as needed

version: 1.0.0
type: tools
name: "Empty Body Test - Tools"
description: "Default tools config for testing empty body handling"
body:
  tools:
    - name: mock-processor
      description: "Mock processor for testing"
