# Tools

All tools operate on `Chapter`, `Paragraph`, or `Line`, with `ToolContext` (LLM, HTTP, vector store, OpenRouter, logger)
and `ToolContext`/`RunContext` (cache, target language, flags).  
Sorted by complexity (Low → High).

| Index | Name                                   | Input           | Output                                               | Category        | Complexity | Description                                                                                                          |
|:------|----------------------------------------|-----------------|------------------------------------------------------|-----------------|------------|----------------------------------------------------------------------------------------------------------------------|
| 1     | `normalizeUnicode`                     | `Line`          | `Line`                                               | Pre‑processing  | Low        | Replace smart quotes, ligatures, diacritics with NFKC form; no API calls.                                            |
| 2     | `trimWhitespace`                       | `Line`          | `Line`                                               | Pre‑processing  | Low        | Remove leading/trailing spaces, collapse multiple spaces, trim empty lines.                                          |
| 3     | `detectEmptyParagraph`                 | `Paragraph`     | `Paragraph & { isEmpty: boolean }`                   | Validation      | Low        | Flags paragraphs that contain only whitespace or zero characters.                                                    |
| 4     | `spellcheckLine`                       | `Line`          | `Line & { typos: Typo[] }`                           | Typo Detection  | Low        | Uses Hunspell WASM dictionary; returns misspelled words + suggestions.                                               |
| 5     | `grammarCheckLine`                     | `Line`          | `Line & { grammarIssues: Issue[] }`                  | Error Detection | Low        | Calls local LanguageTool server (HTTP) to get grammar/style errors.                                                  |
| 6     | `detectRepeatedWords`                  | `Paragraph`     | `Paragraph & { repeated: Repeated[] }`               | Error Detection | Low        | Finds adjacent repeated words (“the the”), common typos via regex.                                                   |
| 7     | `titleCaseChapter`                     | `Chapter`       | `Chapter`                                            | Formatting      | Low        | Converts chapter headings to proper title case (AP style rules).                                                     |
| 8     | `extractFrontmatter`                   | `Chapter`       | `Chapter & { frontmatter: Record<string, unknown> }` | Metadata        | Low        | Parses YAML/TOML frontmatter block from chapter markdown.                                                            |
| 9     | `validateISBN`                         | `Line`          | `Line & { isbnValid: boolean }`                      | Validation      | Low        | Scans line for ISBN‑10/‑13 patterns and validates check digits.                                                      |
| 10    | `detectDeadLinks`                      | `Chapter`       | `Chapter & { deadLinks: Link[] }`                    | Error Detection | Low        | HEAD requests each URL in chapter; reports broken links.                                                             |
| 11    | `checkMarkdownLinks`                   | `Paragraph`     | `Paragraph & { brokenInternalLinks: string[] }`      | Validation      | Low        | Validates internal anchor links (`#section`) against chapter headings.                                               |
| 12    | `countWords`                           | `Line`          | `Line & { wordCount: number }`                       | Metrics         | Low        | Returns word count (split on whitespace) per line.                                                                   |
| 13    | `readabilityScore`                     | `Chapter`       | `Chapter & { fleschKincaid: number }`                | Metrics         | Low        | Computes Flesch‑Kincaid grade level using syllable detection.                                                        |
| 14    | `detectPassiveVoice`                   | `Line`          | `Line & { passiveCount: number }`                    | Style           | Low        | Simple regex for “is/was/were + past participle” patterns.                                                           |
| 15    | `findWeaselWords`                      | `Line`          | `Line & { weaselWords: string[] }`                   | Style           | Low        | Lists occurrences of “might”, “some”, “perhaps”, “maybe”, etc.                                                       |
| 16    | `normalizeLineEndings`                 | `Line`          | `Line`                                               | Pre‑processing  | Low        | Converts CR/LF/CRLF to LF consistently.                                                                              |
| 17    | `removeBOM`                            | `Line`          | `Line`                                               | Pre‑processing  | Low        | Strips UTF‑8 BOM from first line if present.                                                                         |
| 18    | `detectProfanity`                      | `Line`          | `Line & { profanityMatches: string[] }`              | Content Filter  | Low        | Checks line against wordlist; flags offensive terms.                                                                 |
| 19    | `extractEmails`                        | `Paragraph`     | `Paragraph & { emails: string[] }`                   | Extraction      | Low        | Regex extraction of email addresses from text.                                                                       |
| 20    | `detectUrls`                           | `Line`          | `Line & { urls: string[] }`                          | Extraction      | Low        | Finds HTTP/HTTPS URLs in line.                                                                                       |
| 21    | `splitLongParagraph`                   | `Paragraph`     | `Paragraph[]`                                        | Pre‑processing  | Low        | Splits paragraph into multiple paragraphs at sentence boundaries when length > 500 chars.                            |
| 22    | `joinShortLines`                       | `Chapter`       | `Chapter`                                            | Formatting      | Low        | Merges consecutive lines that are under 40 chars (likely line breaks in markdown).                                   |
| 23    | `addLineNumbers`                       | `Line`          | `Line & { lineNumber: number }`                      | Metadata        | Low        | Annotates each line with its index inside the paragraph.                                                             |
| 24    | `detectNumericLists`                   | `Paragraph`     | `Paragraph & { isNumberedList: boolean }`            | Structure       | Low        | Checks if paragraph starts with “1. ”, “2. ” etc.                                                                    |
| 25    | `capitalizeSentences`                  | `Paragraph`     | `Paragraph`                                          | Formatting      | Low        | Ensures first letter of each sentence is capitalised (basic regex).                                                  |
| 26    | `translateLine`                        | `Line`          | `Line`                                               | Translation     | Medium     | Uses `ToolContext.translator` (LibreTranslate or OpenRouter) to translate line to target language from `RunContext`. |
| 27    | `rewriteTone`                          | `Paragraph`     | `Paragraph`                                          | Rewriting       | Medium     | Calls `ToolContext.llm` with system prompt to change tone: academic → conversational, formal → friendly.             |
| 28    | `callOpenRouterAgent`                  | `Paragraph`     | `Paragraph & { agentResult: unknown }`               | Agent           | Medium     | Sends paragraph to an OpenRouter‑hosted agent (e.g., fact‑checker, summarizer). Stores output in metadata.           |
| 29    | `routeToRouter`                        | `Line`          | `Line & { nextTool: string }`                        | Router          | Medium     | Rule‑based: if line contains “TODO” → route to `rewriteTodo`; if citation → `expandCitation`.                        |
| 30    | `extractKeyPhrases`                    | `Chapter`       | `Chapter & { keywords: string[] }`                   | NLP             | Medium     | Pure TS TF‑IDF or YAKE implementation; returns top 10 phrases.                                                       |
| 31    | `embedForRAG`                          | `Chapter`       | `Chapter & { embedding: number[] }`                  | Indexing        | Medium     | Uses `ToolContext.embedder` (Transformers.js) to create vector; stores in `RunContext.vectorStore`.                  |
| 32    | `semanticSimilarity`                   | `Chapter`       | `Chapter & { similarityToPrevious: number }`         | Analysis        | Medium     | Compares embedding of current chapter with previous one; outputs cosine similarity.                                  |
| 33    | `detectPlagiarism`                     | `Paragraph`     | `Paragraph & { duplicateSources: string[] }`         | Validation      | Medium     | Hashes paragraph and checks against `RunContext.fingerprintCache` (local dedup).                                     |
| 34    | `summarizeChapter`                     | `Chapter`       | `Chapter & { summary: string }`                      | Summarization   | Medium     | Uses LLM (lightweight, e.g., Mistral via Ollama) to generate 2‑sentence summary.                                     |
| 35    | `detectHallucination`                  | `Chapter`       | `Chapter & { hallucinationScore: number }`           | Validation      | Medium     | Checks claims against a knowledge base or uses self‑consistency with multiple LLM queries.                           |
| 36    | `expandContractions`                   | `Line`          | `Line`                                               | Pre‑processing  | Low        | Expands “don’t” → “do not”, “it’s” → “it is” (custom mapping).                                                       |
| 37    | `detectGenderBias`                     | `Paragraph`     | `Paragraph & { biasTerms: string[] }`                | Style           | Low        | Finds “policeman”, “fireman”, “chairman” and suggests alternatives.                                                  |
| 38    | `checkConsistencyGlossary`             | `Chapter`       | `Chapter & { glossaryViolations: string[] }`         | Validation      | Low        | Ensures preferred term from glossary is used (e.g., “e‑mail” not “email”).                                           |
| 39    | `extractQuotes`                        | `Paragraph`     | `Paragraph & { quotes: string[] }`                   | Extraction      | Low        | Finds text between single/double quotes or guillemets.                                                               |
| 40    | `detectEllipsisStyle`                  | `Line`          | `Line & { ellipsisOk: boolean }`                     | Style           | Low        | Warns if “...” not used as a single character `…` or spaced incorrectly.                                             |
| 41    | `fixOrdinalNumbers`                    | `Line`          | `Line`                                               | Formatting      | Low        | Replaces “1st”, “2nd”, “3rd” with “first”, “second”, “third” (configurable).                                         |
| 42    | `redactPII`                            | `Paragraph`     | `Paragraph`                                          | Privacy         | Low        | Masks phone numbers, email addresses, SSN patterns with `[REDACTED]`.                                                |
| 43    | `detectAdverbs`                        | `Line`          | `Line & { adverbCount: number }`                     | Style           | Low        | Lists “‑ly” adverbs (quickly, happily) for style suggestions.                                                        |
| 44    | `checkSerialComma`                     | `Paragraph`     | `Paragraph & { missingSerialComma: boolean }`        | Style           | Low        | Detects missing Oxford comma in lists of three or more items.                                                        |
| 45    | `extractAcronyms`                      | `Chapter`       | `Chapter & { acronyms: Record<string, string> }`     | Extraction      | Low        | Finds definitions like “(API)” and builds acronym table.                                                             |
| 46    | `convertNumberWords`                   | `Line`          | `Line`                                               | Formatting      | Low        | Turns “twenty‑five” → “25” (or reverse) based on `RunContext.style`.                                                 |
| 47    | `detectDuplicateParagraphs`            | `Chapter`       | `Chapter & { duplicates: number[][] }`               | Error Detection | Low        | Hashes paragraph content; reports indices of identical paragraphs.                                                   |
| 48    | `splitByHeading`                       | `Chapter`       | `Chapter[]`                                          | Pre‑processing  | Low        | Splits a chapter into sub‑chapters at Markdown heading levels (`##`).                                                |
| 49    | `validateWordCountTarget`              | `Chapter`       | `Chapter & { withinTarget: boolean }`                | Validation      | Low        | Compares word count against `RunContext.wordTarget` (min/max).                                                       |
| 50    | `generateAltText`                      | `Paragraph`     | `Paragraph` (image descriptions)                     | Accessibility   | Medium     | Uses local vision model (Moondream) or LLM to describe images embedded in paragraph.                                 |
| 51    | `translateWithContext`                 | `Paragraph`     | `Paragraph`                                          | Translation     | Medium     | Uses running context (previous 2 paragraphs) to improve translation accuracy via LLM.                                |
| 52    | `expandAbbreviations`                  | `Paragraph`     | `Paragraph`                                          | Pre‑processing  | Low        | Replaces common abbrevs: “e.g.” → “for example”, “i.e.” → “that is”.                                                 |
| 53    | `detectLongSentences`                  | `Line`          | `Line & { longSentences: number[] }`                 | Style           | Low        | Flags sentences > 30 words, suggests breaking.                                                                       |
| 54    | `sentenceSplitter`                     | `Paragraph`     | `Line[]` (sentence‑based)                            | Pre‑processing  | Low        | Converts paragraph into array of `Line` objects, one per sentence.                                                   |
| 55    | `detectWeaselTags`                     | `Line`          | `Line & { weaselTags: string[] }`                    | Style           | Low        | Finds “some experts say”, “it is believed that”, “many people think”.                                                |
| 56    | `convertSmartPunctuation`              | `Line`          | `Line`                                               | Pre‑processing  | Low        | Replaces curly quotes, apostrophes, em‑dashes with straight equivalents (or vice versa).                             |
| 57    | `extractFootnotes`                     | `Chapter`       | `Chapter & { footnotes: Footnote[] }`                | Extraction      | Low        | Parses markdown `[^1]` style footnotes and their definitions.                                                        |
| 58    | `validateReferenceFormat`              | `Paragraph`     | `Paragraph & { refErrors: string[] }`                | Validation      | Low        | Checks that citations (`[Author, year]`) match a bibliography.                                                       |
| 59    | `detectClichés`                        | `Line`          | `Line & { clichés: string[] }`                       | Style           | Low        | Checks line against a list of 200 common clichés.                                                                    |
| 60    | `convertRomanNumerals`                 | `Line`          | `Line`                                               | Formatting      | Low        | Converts Roman numerals (IV, XIX) to integers, configurable on/off.                                                  |
| 61    | `detectMissingSpaces`                  | `Line`          | `Line & { missingSpaces: number[][] }`               | Error Detection | Low        | Finds “end.Start” or “word.Word” patterns missing space.                                                             |
| 62    | `fixStraightQuotes`                    | `Line`          | `Line`                                               | Formatting      | Low        | Replaces straight quotes with curly quotes for proper typography.                                                    |
| 63    | `detectHeadingLevelSkip`               | `Chapter`       | `Chapter & { levelSkips: number[] }`                 | Structure       | Low        | Warns if markdown heading jumps from H2 to H4 (skipping H3).                                                         |
| 64    | `addYAMLFrontmatter`                   | `Chapter`       | `Chapter` (if missing)                               | Metadata        | Low        | Inserts default frontmatter block with creation date and status from `RunContext`.                                   |
| 65    | `checkReadingTime`                     | `Chapter`       | `Chapter & { readingTimeMinutes: number }`           | Metrics         | Low        | Calculates based on 200‑250 wpm.                                                                                     |
| 66    | `detectRedundantPhrases`               | `Paragraph`     | `Paragraph & { redundancies: string[] }`             | Style           | Low        | Finds “advance planning”, “end result”, “free gift”.                                                                 |
| 67    | `extractCharacterNames`                | `Chapter`       | `Chapter & { characters: string[] }`                 | NLP             | Low        | Uses proper noun detection (capitalized words not at start of sentence).                                             |
| 68    | `convertDateFormats`                   | `Paragraph`     | `Paragraph`                                          | Formatting      | Low        | Normalizes “01/02/2024” to “2 January 2024” based on locale config.                                                  |
| 69    | `detectAmbiguousPronouns`              | `Paragraph`     | `Paragraph & { ambiguousRefs: string[] }`            | Style           | Medium     | Uses simple co‑reference heuristics to find “it”, “they” without clear antecedent.                                   |
| 70    | `keywordDensity`                       | `Chapter`       | `Chapter & { density: Record<string, number> }`      | Metrics         | Low        | Reports frequency of top 20 keywords (excluding stopwords).                                                          |
| 71    | `checkCodeBlockLanguage`               | `Chapter`       | `Chapter & { unmarkedCodeBlocks: number[] }`         | Validation      | Low        | Finds markdown code fences without language specifier.                                                               |
| 72    | `detectTrailingSpaces`                 | `Line`          | `Line & { hasTrailingSpace: boolean }`               | Formatting      | Low        | Flags lines ending with spaces (annoying in markdown).                                                               |
| 73    | `detectUnescapedChars`                 | `Line`          | `Line & { unescaped: string[] }`                     | Error Detection | Low        | Finds asterisks, underscores inside words without escaping (`*word*`).                                               |
| 74    | `checkInternalConsistencyNames`        | `Chapter`       | `Chapter & { nameVariants: string[][] }`             | Error Detection | Medium     | Groups possible same entities: “Jon”, “John”, “Jonathan” – suggests unification.                                     |
| 75    | `extractTOC`                           | `Chapter`       | `Chapter & { toc: TocEntry[] }`                      | Extraction      | Low        | Builds table of contents from markdown headings.                                                                     |
| 76    | `detectOverusedWords`                  | `Chapter`       | `Chapter & { overused: Record<string, number> }`     | Style           | Low        | Flags words used more than `n` times per chapter (configurable, e.g., “very”, “really”).                             |
| 77    | `addLanguageTag`                       | `Line`          | `Line & { detectedLang: string }`                    | Metadata        | Medium     | Uses `cld3` or `franc` in TS to detect language per line.                                                            |
| 78    | `convertDialoguePunctuation`           | `Paragraph`     | `Paragraph`                                          | Formatting      | Low        | Replaces double quotes with single for British English (or configurable).                                            |
| 79    | `splitRunonSentences`                  | `Paragraph`     | `Paragraph`                                          | Pre‑processing  | Low        | Splits sentences joined by “, and” or “, but” into separate sentences.                                               |
| 80    | `detectMissingSubject`                 | `Line`          | `Line & { missingSubject: boolean }`                 | Grammar         | Medium     | Simple heuristic: sentence starting with verb (imperative detection with exceptions).                                |
| 81    | `extractStatistics`                    | `Chapter`       | `Chapter & { stats: ChapterStats }`                  | Metrics         | Low        | Returns char count, sentence count, avg word length, etc.                                                            |
| 82    | `detectFillerWords`                    | `Line`          | `Line & { fillers: string[] }`                       | Style           | Low        | Finds “um”, “uh”, “like”, “actually”, “basically”.                                                                   |
| 83    | `convertElision`                       | `Line`          | `Line`                                               | Formatting      | Low        | Expands “gonna” → “going to”, “wanna” → “want to”.                                                                   |
| 84    | `checkHtmlEntities`                    | `Line`          | `Line & { unescapedEntities: string[] }`             | Validation      | Low        | Finds `&` not followed by `&amp;` in non‑code contexts.                                                              |
| 85    | `detectRelativeTime`                   | `Paragraph`     | `Paragraph & { relativeTimes: string[] }`            | Error Detection | Low        | Flags “tomorrow”, “yesterday”, “next week” (may date the book).                                                      |
| 86    | `extractGlossaryTerms`                 | `Chapter`       | `Chapter & { terms: string[] }`                      | Extraction      | Low        | Builds list of bold/italic terms that might need glossary entries.                                                   |
| 87    | `capitalizeHeadings`                   | `Chapter`       | `Chapter`                                            | Formatting      | Low        | Converts heading text to proper case (configurable: title case or sentence case).                                    |
| 88    | `detectLeadingWhitespace`              | `Line`          | `Line & { indentLevel: number }`                     | Structure       | Low        | Counts leading spaces or tabs for code block detection.                                                              |
| 89    | `removeDuplicateSpaces`                | `Line`          | `Line`                                               | Pre‑processing  | Low        | Replaces multiple spaces with single space (except within code blocks).                                              |
| 90    | `extractUUIDs`                         | `Line`          | `Line & { uuids: string[] }`                         | Extraction      | Low        | Finds UUID‑v4 patterns in text.                                                                                      |
| 91    | `detectSentenceSpacing`                | `Line`          | `Line & { doubleSpaceAfterPeriod: boolean }`         | Style           | Low        | Flags double spaces after period (old‑style typography).                                                             |
| 92    | `convertFractionToUnicode`             | `Line`          | `Line`                                               | Formatting      | Low        | Changes “1/2” → “½”, “3/4” → “¾” (configurable).                                                                     |
| 93    | `detectMissingAltText`                 | `Chapter`       | `Chapter & { imagesWithoutAlt: string[] }`           | Accessibility   | Low        | Finds markdown images `![]()` missing alt text inside brackets.                                                      |
| 94    | `addSoftHyphens`                       | `Line`          | `Line`                                               | Typesetting     | Low        | Adds `&shy;` after certain syllables for better line breaking (naive).                                               |
| 95    | `detectURLShorteners`                  | `Line`          | `Line & { shortenedURLs: string[] }`                 | Security        | Low        | Flags `bit.ly`, `tinyurl`, `goo.gl` links for expansion.                                                             |
| 96    | `extractPullQuotes`                    | `Chapter`       | `Chapter & { pullQuotes: string[] }`                 | Extraction      | Low        | Finds sentences enclosed in `> ` blockquotes.                                                                        |
| 97    | `checkEmojiUsage`                      | `Line`          | `Line & { emojis: string[] }`                        | Content         | Low        | Lists emoji characters; optionally warns if outside designated sections.                                             |
| 98    | `normalizeWhitespaceAroundPunctuation` | `Line`          | `Line`                                               | Pre‑processing  | Low        | Removes space before comma/period, adds space after comma.                                                           |
| 99    | `detectMicrosoftSmartTags`             | `Paragraph`     | `Paragraph & { smartTags: string[] }`                | Validation      | Low        | Finds leftover `<st1:...>` tags from Word export.                                                                    |
| 100   | `extractResearchQuestions`             | `Chapter`       | `Chapter & { questions: string[] }`                  | Extraction      | Low        | Finds sentences ending with `?` (potential reader questions).                                                        |
| 101   | `detectTautology`                      | `Line`          | `Line & { tautologies: string[] }`                   | Style           | Low        | Finds “added bonus”, “future plans”, “past history”.                                                                 |
| 102   | `checkDialogueTagVariety`              | `Chapter`       | `Chapter & { tagOveruse: Record<string, number> }`   | Style           | Low        | Counts “said”, “asked”, “replied”, warns if one dominates.                                                           |
| 103   | `convertNonBreakingSpaces`             | `Line`          | `Line`                                               | Pre‑processing  | Low        | Replaces `&nbsp;` with normal space, except in code blocks.                                                          |
| 104   | `detectLazyWord`                       | `Line`          | `Line & { lazyWordCount: number }`                   | Style           | Low        | Flags “thing”, “something”, “stuff”, “nice”, “good” without specifics.                                               |
| 105   | `extractTemporalExpressions`           | `Paragraph`     | `Paragraph & { timex: string[] }`                    | Analysis        | Medium     | Uses simple patterns (“in 2024”, “during the 19th century”) for timeline extraction.                                 |
| 106   | `chunkChapter`                         | `Chapter`       | `Chapter[]`                                          | Pre‑processing  | Medium     | Splits chapter > 12000 tokens into smaller parts for LLM processing.                                                 |
| 107   | `extractEntities`                      | `Chapter`       | `Chapter & { entities: Entity[] }`                   | NLP             | Medium     | Finds and marks named entities (persons, places, organizations) in text.                                             |
| 108   | `consolidateSummaries`                 | `Summary[]`     | `Summary`                                            | Summarization   | Medium     | Merges multiple chapter summaries into a consolidated summary.                                                       |
| 109   | `generatePlotDiagram`                  | `Chapter`       | `Chapter & { plotDiagram: PlotNode[] }`              | Visualization   | High       | Creates visual plot diagram showing narrative structure, conflicts, and character arcs.                              |
| 110   | `detectScenes`                         | `Chapter`       | `Chapter & { scenes: Scene[] }`                      | Analysis        | Medium     | Parses chapter into scenes based on time/location changes or action beats.                                           |
| 111   | `generateImage`                        | `Chapter`       | `Scene`                                              | Summary`        | `Image`    | Media                                                                                                                | High       | Uses AI image generation (e.g., Flux) to create visual for chapter/scene/summary.                                  |
| 112   | `generateAudio`                        | `Chapter`       | `Scene`                                              | Summary`        | `Audio`    | Media                                                                                                                | High       | Uses TTS (machinum-tts) to add audio narration for content.                                                        |
| 113   | `exportToVideo`                        | `Image & Audio` | `Video`                                              | Media           | High       | Combines generated image with audio track to produce MP4 video.                                                      |
| 114   | `indexWithLightRAG`                    | `Chapter`       | `Chapter & { ragDocId: string }`                     | Indexing        | High       | Indexes chapter content in LightRAG vector store for retrieval.                                                      |
| 115   | `synthesizeSpeech`                     | `Text`          | `Audio`                                              | Media           | Medium     | Uses machinum-tts for high-quality speech synthesis.                                                                 |
| 116   | `runAutomataWorkflow`                  | `Chapter`       | `Chapter & { automataResult: unknown }`              | Agent           | High       | Executes automation workflow via machinum-automata.                                                                  |
