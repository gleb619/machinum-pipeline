{
  "name": "sample1",
  "version": "1.0.0",
  "description": "Machinum Pipeline sample — HTTP to JSONL integration test",
  "type": "module",
  "scripts": {
    "runner": "NODE_OPTIONS='--import tsx' mt run ./pipelines/http-to-jsonl.ts",
    "runner:http": "NODE_OPTIONS='--import tsx' mt run ./pipelines/http-to-jsonl-example.ts",
    "runner:md": "NODE_OPTIONS='--import tsx' mt run ./pipelines/jsonl-to-md.ts",
    "simulate": "tsx simulation1.ts",
    "example": "mkdir -p vendor && pnpm -C ../../packages/core pack --pack-destination \"$(pwd)/vendor\" && pnpm -C ../../packages/cli pack --pack-destination \"$(pwd)/vendor\" && pnpm -C ../../packages/waypoint pack --pack-destination \"$(pwd)/vendor\" && pnpm -C ../../packages/tools pack --pack-destination \"$(pwd)/vendor\" && npm install --no-save --no-audit --no-fund file:./vendor/mt-core-0.1.0.tgz file:./vendor/mt-cli-0.1.0.tgz file:./vendor/mt-waypoint-0.1.0.tgz file:./vendor/mt-tools-0.1.0.tgz && tsx run-example.ts",
    "cleanup": "rm -rf jsonl/output.jsonl .mt vendor package-lock.json node_modules pipelines/http-to-jsonl-example.ts chapters/en/*.md",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
