{
  "name": "sample0",
  "version": "1.0.0",
  "description": "Machinum Pipeline sample — mt init integration test",
  "type": "module",
  "scripts": {
    "example": "mkdir -p vendor && pnpm -C ../../packages/core pack --pack-destination \"$(pwd)/vendor\" && pnpm -C ../../packages/cli pack --pack-destination \"$(pwd)/vendor\" && pnpm -C ../../packages/waypoint pack --pack-destination \"$(pwd)/vendor\" && npm install --no-save --no-audit --no-fund file:./vendor/mt-core-0.1.0.tgz file:./vendor/mt-cli-0.1.0.tgz file:./vendor/mt-waypoint-0.1.0.tgz && npx mt init sample-project",
    "cleanup": "rm -rf sample-project vendor package-lock.json node_modules",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
