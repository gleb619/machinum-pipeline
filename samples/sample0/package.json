{
  "name": "sample0",
  "version": "1.0.0",
  "description": "Machinum Pipeline sample — mt init integration test",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
