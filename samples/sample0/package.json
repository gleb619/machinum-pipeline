{
  "name": "sample0",
  "version": "1.0.0",
  "description": "Machinum Pipeline sample — mt init integration test",
  "type": "module",
  "scripts": {
    "example": "mt init sample-project",
    "cleanup": "rm -rf sample-project .mt mt.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mt/core": "workspace:*",
    "@mt/cli": "workspace:*",
    "@mt/waypoint": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
