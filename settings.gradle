rootProject.name = "machinum"

include(
    "core",
    "cli",
    "server",
    "tools:common",
    "tools:internal",
    "tools:external"
)

// Faster configuration caching
enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")
include 'server:admin-backend'
include 'server:http-streamer'